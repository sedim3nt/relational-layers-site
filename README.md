# Relational Layers

Relational Layers helps a person decide whether to deepen, hold, clarify, or step back in a relationship. The site keeps the model narrow, stores the session locally, and sends product feedback to Airtable.

## Local development

```bash
npm install
npm run dev
```

## Environment

See `.env.example`.
